# Exercise profiles as copied recipes

OVRLOAD stores user-owned Custom Profiles and system-owned OVRLOAD Presets as reusable recipes, but materializes their values into routines and blocks rather than live-linking existing workout plans. Supersets may assign Target/Floor profiles per exercise while retaining shared warm-up and Rest at block level; published system presets are immutable so later additions cannot silently rewrite user programming.
